#include <stdio.h>

int main() 
{
    /* Cesar mendez
    equipo 1
    21 abr 21
    */

   int a, b, c; //recibe valores enteros.
   printf ("/n /n /nPrograma 2/n");
   printf ("Suma de numeros neteros /n");
   printf("/n /nDame el valor de a; ");
   scanf ("%d", &a); //si vas a recibir un valor entero usa %d
                     // para indicar la variable donde se guarda usa &variable 
    printf ("/n /nDame el valor de b: ");
    scanf ("%d",&a);
    c = a + b;
printf ("/n El resultado es &d",c)
   return 0;
} 